# Changelog

## 1.7.0 - 2026-08-15

- 修复首次打开时用户可在资料库读取完成前新建内容，随后被迟到的初始数据覆盖的竞态。
- 资料库尚未完成初始化时显示专用打开状态，加载完成后才开放创作、归档等工作区操作。
- 新增主动延迟初始资料库响应的回归，确认加载期不可新建，加载后仍保持服务端返回前可编辑。

## 1.6.0 - 2026-08-15

- 将“新建内容立即可编辑”回归改为确定性阻塞服务端响应，不再依赖 GitHub Runner 必须在 500ms 内渲染。
- 发布流程继续要求卡片必须在服务端创建返回前出现且可编辑，消除机器速度导致的偶发误报。

## 1.5.0 - 2026-08-15

- 修复全新 macOS 环境只有 Electron npm 包、尚未下载 Electron 应用二进制时桌面打包失败的问题。
- 桌面打包前按目标架构自检 Electron 运行时，缺失或架构不符时自动下载并再次校验。
- 保持本地缓存可直接复用，换电脑首次构建和 GitHub Runner 使用同一条自恢复打包链路。

## 1.4.0 - 2026-08-15

- 修复全新 GitHub Runner 上没有预置 QA 资料库时界面回归统一返回 503 的问题。
- 仅在显式 QA 模式下自动创建隔离资料库，生产资料库继续保持禁止隐式创建的保护规则。
- 允许自动化通过环境变量指定全新的隔离资料库根目录，避免本地遗留数据掩盖首次启动问题。
- 为异步自动保存和路径安全回归提供适合共享 Runner 的等待窗口，不放宽结果断言。

## 1.3.0 - 2026-08-15

- 完成本地最新版向 GitHub 主分支的发布收口，应用内用户版本升级为 V1.3。
- 统一桌面构建、安装和 GitHub Release 的应用名称为 Video Hub。
- 安装新版时安全替换同 Bundle ID 的旧中文应用副本，并保留原资料库会话与平台登录状态。
- 保留项目固定的 Eagle 素材库连接和素材文件夹路由，确保团队电脑使用同一套关联规则。

## 1.2.0 - 2026-08-14

- 创作台的博主号与 IP 号正文区增加账号编辑图标，可直接进入对应内容编辑页。
- 原“创作”页面更名为“编辑”，支持按账号独立编排标题、正文、封面和视频素材。
- 编辑页与创作台共享同一项目数据；完成编辑后同步返回创作台，灵感关联和分类保持共享。
- 应用内用户版本升级为 V1.2。

## 1.1.0 - 2026-08-12

- Eagle 媒体永久关联改为以 item ID 为核心；资产移动文件夹后仍可读取和播放。
- Eagle item 被手动删除时显示稳定失效状态；软件删除只解除索引，不调用 Eagle 删除。
- 发布桌面应用版本 1.1.0。

## Unreleased

- 建立版本批次开发流程，公开 `main` 只保留完成真实应用验收的稳定版本。
- 应用内显示版本号、Git commit 和未提交状态。
- 增加发布元数据检查和 GitHub Release 自动构建流程。
- 记录手机异地链接收集、多平台主页扫描、批量采集和逐字稿优先级路线。

## 0.1.0 - 2026-07-31

- 发布 macOS 本地优先桌面应用源码。
- 支持抖音、小红书灵感采集与专用登录会话。
- 支持灵感关联、新建创作、待发布排序和发布归档。
- 支持多封面和博主号/IP 号四个视频槽位。
- 使用内容单元、manifest、媒体角色和相对路径组织 Library。
- 提供 Codex 安装任务书、可重复构建脚本和脱敏界面截图。
- 新用户首次启动不再依赖仓库作者的 NAS 路径。
